# Test Fixtures

This directory contains test data and fixtures for the test suite.

## Structure

- `dummy_policies/` - Sample policy documents for testing
- `expected_outputs/` - Expected analysis results for validation
- `test_configurations/` - Test configuration files

## Usage

Test fixtures are loaded via pytest fixtures defined in `conftest.py`.
